== README

This application features available commercial properties and allows users to suggest and vote
for what business they would like to see in that location.

DEVELOPMENT
1. Bundle Install
2. Create database by running rake db:create
3. Run migrations with rake db:migrate
4. Run migrations in test database with RACK_ENV=test rake db:migrate
5. Run tests using rspec
